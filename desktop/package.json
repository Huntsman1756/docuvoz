{
  "name": "docuvoz-desktop",
  "version": "0.2.0-beta.1",
  "private": true,
  "description": "DocuVoz desktop (Tauri) static frontend. Shares all UI and logic with the root web app via ../src; contains NO API routes so `output: export` is honest.",
  "license": "Apache-2.0",
  "engines": {
    "node": "^24.15.0"
  }
}
