# Build / dependency artifacts
.next/
node_modules/
coverage/
playwright-report/
test-results/
public/pdfjs/
sidecar/dist/
desktop/.next/
desktop/out/
desktop/public/pdfjs/
src-tauri/target/
src-tauri/gen/schemas/

# Vendored third-party code and its provenance records are owned upstream and
# must stay byte-identical — never reformatted by the project toolchain.
**/vendor/**

# Generated data (produced by `npm run fixtures` / eval runs), not hand-authored
# source — excluded so the formatter only governs source we own.
public/corpus/reference/
public/corpus/gold/
evaluation/results/
